# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 5 — Dashboard (complete)
**Last completed:** 17 Analytics Charts — Real Data
**Next:** none, all 17 planned features are built, verified, and tested

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [x] 09 Find Jobs Page — Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [x] 12 Job Details Page — Full UI
- [x] 13 Company Research Agent

### Phase 5 — Dashboard

- [x] 14 Dashboard Page — Full UI
- [x] 15 Stats Bar — Real Data
- [x] 16 Recent Activity — Real Data
- [x] 17 Analytics Charts — Real Data

---

## Decisions Made During Build

- Auth uses `@insforge/sdk/ssr` with server owned PKCE cookies.
- Next.js 16 route protection lives in `proxy.ts`.
- OAuth callbacks exchange the InsForge code at `/callback`, then redirect to `/dashboard`.
- The current dashboard page is the authenticated landing shell required to verify the auth redirect. Feature 14 replaces it with the full dashboard UI.
- PostHog initializes through a root client provider and only the four approved product events should be captured by future feature slices.
- Database schema, row level security, and the resumes storage bucket for feature 04 are built, verified, and tested per `docs/specs/0001-database-schema/` (spec 0001, status Accepted) and `docs/specs/0001-database-schema/verify.md`. The migration lives at `migrations/20260718170543_create-core-tables.sql`. `/check verify` proved cross user isolation and account deletion cascade for real, using two throwaway signed in test accounts (created and fully deleted during the run). That cascade is the database row cascade (the `profiles` row, via the FK to `auth.users`) only — feature 07's `/check verify` and `/debug` runs found that deleting a throwaway account does NOT cascade delete its storage objects (any resume PDFs uploaded directly, outside the app's own upload flow, are orphaned and must be deleted explicitly); worth remembering for any future storage cleanup or test account teardown story. `/test` extended `tests/posthog-contract.test.mjs` to lock in this session's other change (PostHog event cleanup); feature 04 itself has no application code surface to test, its correctness lives entirely in the verify evidence.
- `/check verify` ran against 02 Auth (no formal spec exists for it, this project's build plan served as the checklist instead): confirmed for real, via direct HTTP against the running dev server, that the login page renders both provider buttons, clicking each one redirects to the real Google or GitHub OAuth authorize URL with the correct client id and scopes, the PKCE verifier cookie is set correctly, all four private routes are middleware protected, and the callback route fails closed (no crash, no leaked internals) on a missing, fake, or tampered code or verifier. The one thing left unverified: actually completing a real third party login end to end, since no browser automation tool is configured in this environment and that requires live Google or GitHub test credentials. Checked off on that basis; if a real login is ever completed and watched by a person, that closes the last gap.
- 05 Profile Page built as `app/profile/page.tsx` (auth-gated, same pattern as `app/dashboard/page.tsx`) composing three new components: `components/profile/CompletionIndicator.tsx` (needs-attention banner with an inline SVG completion ring), `components/profile/ResumeUpload.tsx` (client, drag-and-drop dropzone + Generate Resume button, no upload wiring yet), and `components/profile/ProfileForm.tsx` (client, single `Profile` state object covering Personal Info, Professional Info with tag inputs for skills/industries, Work Experience up to 3 entries, Education, and Job Preferences; Save Profile button not yet wired). Added `types/index.ts` (`Profile`, `ProfileCompletion`, and related field types) and the `lucide-react` dependency (already pre-approved in `code-standards.md`, just not installed yet). Mock data only, matching `context/designs/profile.png`; no save logic (feature 06).
- Visually verified against `context/designs/profile.png` using a Playwright screenshot: launched an isolated dev server on port 3005 (left the user's own port 3000 server untouched), briefly disabled the `/profile` auth gate in `proxy.ts` and `app/profile/page.tsx` for that isolated server only, screenshotted, then immediately reverted both files (confirmed via `git diff`) and tore down the port 3005 server. No console errors. The rendered page matches the design closely. Fixed one real gap found this way: the design's navbar has no Sign out button on the profile page, so `Navbar` is now called with `showAuthAction={false}` there. Two things intentionally NOT changed because they're outside this feature's scope: the design shows small icons and an underlined active state on nav items, but the shared `Navbar` component (used by every authenticated page) has neither, and `ui-rules.md` explicitly says "No underline — active state is color change only" — reconciling that is a cross-cutting `Navbar`/`ui-rules.md` decision, not a Profile-page one. Also, the design has no Cover Letter Tone field under Job Preferences even though `build-plan.md` and the `profiles` DB schema mention one — built to match the screenshot per the design-is-source-of-truth rule; flag this when the field is actually needed (feature 06 or later).
- `/check verify` re-verified 05 with a real InsForge session instead of a code bypass: temporarily disabled `require_email_verification` via `npx @insforge/cli config apply` (planned and confirmed via `config plan` first, restored afterward and re-exported to confirm), signed up one throwaway account, cookie-injected its access token into a Playwright session against the user's own running dev server (port 3000, untouched otherwise), confirmed unauthenticated `/profile` still 307s to `/login`, and confirmed the authenticated render, the Skills tag add, the Work Experience "Add role" append, and the "Currently working here" end-date toggle all behave correctly with zero console errors. The throwaway account (and one leftover unverified account from the earlier build-time screenshot check) were deleted afterward. `/test` added `tests/profile-contract.test.mjs` (18 tests, source-contract style matching this project's existing `*-contract.test.mjs` convention since no jsdom/testing-library is configured in `test-preferences.json`): the auth redirect, the no-sign-out-button and no-premature-save-wiring regression guards, the work experience 3-entry cap, the currently-working-here disable wiring, skills/industries dedupe, and a hardcoded-hex/raw-Tailwind-color invariant check across all four changed files. Full suite (`npm test`): 37/37 passing.
- 06 Profile Save Logic designed via `/architect` and recorded at `docs/specs/0002-profile-save-logic/` (spec 0002, status Proposed, not yet built). Decision: one combined Server Action, `saveProfile(profile, resumeFile)` in `actions/profile.ts`, that saves every form field and, when a new resume was selected, uploads it too, all in a single call triggered only by the Save Profile button. Revised in session from an initial "upload the resume immediately on select" design after the engineer asked directly for the resume to save together with the rest of the form. A new client component, `components/profile/ProfileEditor.tsx`, wraps `ResumeUpload` and `ProfileForm` to hold the selected file until Save Profile is clicked, since the two are separate sibling components. Completion percentage/missing fields are derived at read time from 10 required fields (no migration, no new columns), chosen because that exact set reproduces the design's 70%/PHONE-LOCATION-EDUCATION example precisely. Cover Letter Tone stays out of scope.
- Spec 0002's storage design revised a second time during `/develop`'s own pre build check: directly tested (not assumed) the live `PUT /api/storage/buckets/:bucket/objects/:key` endpoint the SDK's `upload()` actually calls, by uploading twice to the same key. Confirmed storage never overwrites, it silently renames on collision, and an unauthenticated fetch of the returned `url` gets a 401 (matches the bucket being private). Final design: each resume upload targets a fresh unique key rather than a fixed path, `saveProfile` reads the caller's current row first (supplies the previous resume key plus the previous `is_complete` for the `profile_completed` transition check), and a replace is upload new, write it, then delete the old key, so a failed upload or write never costs the user their existing resume. A cross check on a different model (Opus) caught one real gap in that revision (nothing said where the "previous key to delete" came from) before it went further, fixed by the row read above.
- 06 Profile Save Logic built via `/develop` against spec 0002: `types/index.ts` gained `ProfileRow`, `ProfileWritePayload`, `ActionResult<T>`, `ProfileCompletionInput`; `lib/profile-completion.ts` and `lib/profile-mapping.ts` are the shared pure helpers; `actions/profile.ts` has the one `saveProfile(profile, resumeFile)` Server Action; `components/profile/ProfileEditor.tsx` is new, wraps `ResumeUpload` and `ProfileForm` and owns the shared `profile`/`resumeFile` state (`ProfileForm` is now a fully controlled component, no longer holding its own profile state); `app/profile/page.tsx` fetches the real row and renders `ProfileEditor` in place of the two components it rendered directly before. Found and fixed one more live SDK mismatch while building, not just designing: `insforge.from(table)` does not exist, the real accessor is `insforge.database.from(table)` (its `.from()` returns a `@supabase/postgrest-js` `PostgrestQueryBuilder`, confirmed via the SDK's own type exports, which is also how `.upsert()` and `.maybeSingle()` were confirmed to exist and work as designed). This is a mechanical fix, not a design change, so it was made directly rather than routed back through `/architect`; `context/library-docs.md`'s DB Queries example needs the same correction, on top of the already flagged Storage correction, next time `/sync` runs.
- `/check verify` ran against 06 with two real InsForge sessions (throwaway accounts, both deleted afterward): confirmed for real, via a real save/reload cycle, that every field persists, `job_titles_seeking` round trips through the comma split and join including a double-comma edge case, completion goes from a real 0% (all ten fields missing) to a real 100% with `is_complete: true` in the database, a resume upload gets a fresh unique key and a replace leaves exactly one live object in storage (the old one actually deleted, confirmed via `storage list-objects`, not assumed), and selecting a resume without clicking Save Profile leaves both the database and storage completely untouched. One item was genuinely blocked, not faked: whether the `profile_completed` PostHog event was actually received, since this environment only has a write only project key, no query capable one; the database side of that same transition (`is_complete` flipping false to true exactly once) was confirmed directly. Verdict: PASS. Also surfaced, not a feature 06 defect: the `Experience Level`, `Work Authorization`, and `Highest Degree` dropdowns (built in feature 05) visually showed their first option when genuinely unset; fixed in the loose-end cleanup below.
- `/test` added `tests/profile-completion.test.mjs` and `tests/profile-mapping.test.mjs` (22 real unit tests calling the actual pure functions, including a full round trip test and the exact design mock's 70% scenario), and extended `tests/profile-contract.test.mjs` (18 new tests, 36 total) to cover the new `ProfileEditor` wiring and a thorough contract for `actions/profile.ts` (auth check ordering, validate-before-upload, read-before-write, the unique key design, delete-only-after-write-succeeds, the `profile_completed` transition guard, the `insforge.database.from` accessor). Two tests that were deliberate "feature 06 hasn't started yet" regression guards were updated to assert the real, now-shipped behavior instead of being left permanently red. Full suite: 77/77 passing. `npx tsc --noEmit` and `npm run lint` both clean.
- Feature 06 is done: spec 0002 is `Accepted`, `/check verify` and `/test` both passed with real evidence. Cover Letter Tone remains explicitly out of scope for feature 06.
- Post-close bug fix, found by the engineer from a screenshot: `CompletionIndicator` had never actually reached a real complete state before (feature 05 only ever showed it against static 70% mock data), so once feature 06 made 100% genuinely reachable it revealed the component always rendered the red "Profile needs attention" heading, icon, and ring, even at 100%. Fixed: `isComplete = missingFields.length === 0` now drives the heading text, icon (`CheckCircle`/`text-success` vs `AlertCircle`/`text-error`), body copy, and ring color together. Verified with a real server render (esbuild-transformed the component, rendered it with `react-dom/server` for both a 100% and a 70% input) before committing to it, not just by reading the code; a regression test was added to `tests/profile-contract.test.mjs` and `ui-registry.md`'s entry updated.
- 06 Profile Save Logic, second revision: the engineer asked to reverse the resume upload timing back to uploading immediately on select (the opposite of the choice made earlier in feature 06 itself), this time keeping the database write deferred to Save Profile. Designed via `/architect` as an in place update to spec 0002 (status moved `Accepted` → `Proposed` → `In Progress`, since the reversal invalidated the original AC-4/AC-8 and needed new code): `actions/profile.ts` splits into a new `uploadResumeFile(file, previousUnsavedKey?)` Server Action (validates and uploads immediately, best effort deletes a previous not yet saved upload) and a slimmed down `saveProfile(profile, resumeKey)` that no longer touches storage at all, only decides whether to write the already resolved key. A cross check on a different model (Opus) caught a real race condition in the first draft of this revision (nothing stopped picking a second file while the first upload was still in flight, which could leak or wrongly overwrite the resolved key); fixed by disabling `ResumeUpload`'s dropzone and file input for the whole time an upload is in flight, the same lock already applied to Save Profile (new AC-9).
- 06 second revision built via `/develop` against the updated spec 0002: `actions/profile.ts` now exports `uploadResumeFile` and a rewritten `saveProfile`; `components/profile/ResumeUpload.tsx` is presentational again but now renders `isUploading`/`uploadedFileName`/`uploadError` and disables its dropzone and file input while uploading; `components/profile/ProfileEditor.tsx` replaced its `resumeFile` state with `resumeKey`/`resumeFileName`/`isUploading`/`uploadError`, calls `uploadResumeFile` the moment a file is selected, and only clears the resolved key once `saveProfile` actually succeeds; `components/profile/ProfileForm.tsx` gained a `saveDisabled` prop, kept separate from `isSaving`, so the Save Profile button disables while an upload is in flight without its text incorrectly reading "Saving…". `tests/profile-contract.test.mjs` updated (old `resumeFile`/`selectedFileName` assertions rewritten; new tests added for the immediate upload call, the reselect cleanup, and the dual disabled state). Full suite: 82/82 passing. `npx tsc --noEmit` and `npm run lint` both clean. `ui-registry.md` updated for `ResumeUpload`, `ProfileForm`, `ProfileEditor`, and `ProfilePage`.
- `/check verify` (first pass) on the second revision found two real bugs by driving the real app: a resume between roughly 1MB and 5MB failed outright, because Next.js's own default Server Action body limit (1MB) rejected it before `uploadResumeFile`'s own 5MB check ever ran; and `ProfileEditor`'s upload promise chain had no `.catch()`, so that rejection left `isUploading` stuck `true` forever, no error shown, Save Profile permanently disabled for the session. `/test` added a regression test for the second bug (left deliberately red, a real bug, not a test mistake) and confirmed the other 82 were still green. `/debug` fixed both: `next.config.ts` gained `experimental.serverActions.bodySizeLimit: "6mb"`, and `ProfileEditor.handleFileSelected` gained a `.catch()` that resets `isUploading` and shows an error on any unexpected rejection. `/debug`'s own live re-check that day also surfaced what looked like a third, deeper issue specific to a 2MB file, isolated (via direct SDK calls and raw HTTP replication) to something in the Next.js Server Action file relay rather than the SDK or S3 — but this was based on a single failure, never retested for determinism before being written up.
- `/check verify` (second pass) re-ran the full range after the fixes: 240 bytes, 1.5MB, and four separate fresh attempts at the exact 2MB file that failed during `/debug` all uploaded successfully; a 6MB file still correctly rejects client side; a full save/reload cycle still round trips every field, the comma edge case, and 100% completion. The "2MB issue" `/debug` reported did not reproduce again and is now understood to have been a one off, likely a transient InsForge presigned upload hiccup, not a deterministic size threshold in this app's code; noted as a correction to `/debug`'s own report, not carried forward as an open defect. `tsc`, lint, and the full suite (83/83, including the new regression test, now green) all clean. Spec conformance: PASS. Feature 06 (both revisions) is done: spec 0002 is `Accepted`.
- `/sync` corrected `context/library-docs.md`'s InsForge sections, stale since feature 06's first revision: DB Queries now shows `insforge.database.from(table)` (the top level `insforge.from(table)` never existed); Storage now shows `.upload(path, file)` taking no options object, storage never overwriting a key (fresh unique key per upload, replace is upload then write then delete the old key, never delete first), and `createSignedUrl()` instead of `getPublicUrl()` since `resumes` is a private bucket (only the key is ever persisted, never a URL). The same stale `upsert: true` pattern in the `@react-pdf/renderer` example (feature 08) was corrected too, so that feature doesn't inherit the same wrong assumption. Root `AGENTS.md`'s own "Key patterns" bullet ("persist both the returned `url` and `key`") was also wrong for this project's private bucket and is now corrected to key only.
- 06 Profile Save Logic, third revision: the engineer found that refreshing the page after selecting a resume but before clicking Save Profile made the file look like it had vanished, even though it was already uploaded to storage. Designed via `/architect` as an in place update to spec 0002 (status moved `Accepted` → `Proposed`, new AC-10), then built via `/develop`: a new `lib/staged-resume-storage.ts` mirrors the staged, not yet saved `resumeKey`/`resumeFileName` into `sessionStorage`, namespaced per user (`profile-staged-resume:${userId}`), written on a successful upload and cleared on a successful save. `sessionStorage`, not `localStorage`, on purpose, tab scoped and cleared on tab close, so no cross tab staleness to reason about. The spec's originally described mechanism (a plain `useState` plus a `useEffect` that calls `setState` to rehydrate on mount) turned out to trip this project's own `react-hooks/set-state-in-effect` lint rule (from `eslint-config-next/core-web-vitals`, unmodified) the moment it was actually built; corrected in place to `useSyncExternalStore`, React's own recommended tool for exactly this "read a browser only value once, hydration safe" case, since a `useState` initializer reading `sessionStorage` would have caused a real hydration mismatch (server renders with no `sessionStorage` at all) and the `useEffect` version is the literal pattern that lint rule exists to catch. Same observable behavior as specced (AC-10 unaffected), a mechanical implementation correction discovered by building it, not a design change, so it was made directly rather than re routed through `/architect`, the same precedent as the earlier `insforge.database.from` fix. `app/profile/page.tsx` now threads a new `userId` prop to `ProfileEditor` (from the session it already reads), since `Profile` itself deliberately still carries no `id` field. A cross check on a different model (Opus) confirmed no way for this design to cause the wrong resume to get saved, and caught one real hydration hazard in the first draft plus two documentation gaps (AC-8/AC-10 wording, confirming `ResumeUpload` needs no new prop), all fixed before the spec was confirmed. `tests/profile-contract.test.mjs` updated (three stale assertions rewritten for the new mechanism) and a new `tests/staged-resume-storage.test.mjs` added (10 real unit tests against the actual functions, including a mock unavailable `sessionStorage` and a subscribe/unsubscribe check). Full suite: 93/93 passing. `npx tsc --noEmit` and `npm run lint` both clean. `ui-registry.md`'s `ProfileEditor` entry updated.
- `/check verify` on the third revision confirmed AC-10 live, with two throwaway accounts (both fully deleted afterward; the real account's own resume objects were identified and left untouched throughout): a staged resume survives a same tab reload with zero hydration warnings, a reload then Save Profile persists the rehydrated key correctly, and a second signed in session in the same tab (cookie swapped, no context restart) never sees the first session's staged file, proving the user id namespacing works even though `sessionStorage` itself is shared at the tab level. A regression pass confirmed AC-1 through AC-9 all still hold after replacing `useState` with `useSyncExternalStore`. One early script run timed out; investigated rather than assumed a bug, and confirmed it was the dev server's cold Turbopack compile on the very first request, not a real defect, by rerunning against a warm server. Commands and the full suite (93/93) all clean. Spec conformance: PASS. `/test` confirmed the suite already written during `/develop` fully traces to AC-10 (no new tests needed) and the three live only checklist items (real reload, real cross session cookie swap) are correctly left to `/check verify`, not faked.
- `/sync` added one durable convention this third revision surfaced: `context/code-standards.md`'s "Next.js 16 Conventions" section now states the project's rule for reading a browser only value (`localStorage`, `sessionStorage`) that affects render output, use `useSyncExternalStore`, never a `useState` initializer or a `useEffect` that calls `setState`, since a Client Component is still server rendered first and either alternative either causes a real hydration mismatch or trips this project's own `react-hooks/set-state-in-effect` lint rule. Feature 06 (all three revisions) is done: spec 0002 is `Accepted`.
- Loose-end cleanup after feature 06: `components/profile/ProfileForm.tsx` now renders real blank placeholder options for Work Authorization, Experience Level, Highest Degree, and Remote Preference so empty profile values no longer look like saved first options. `context/build-plan.md` and `context/architecture.md` now use the fresh unique resume key design (`resumes/{user_id}/{random}.pdf`) instead of stale fixed `resume.pdf` wording. Cover Letter Tone is resolved for now as intentionally absent from the profile UI, matching the delivered design; the `cover_letter_tone` column remains a reserved nullable DB column. Navbar icons/underlines are resolved in favor of the shared component and `ui-rules.md`: text-only links with color-only active state. Orphan cleanup for staged-but-never-saved resume uploads remains a future storage-maintenance feature, not part of feature 06.
- 07 AI Profile Extraction from Resume designed via `/architect` and recorded at `docs/specs/0003-ai-profile-extraction-from-resume/index.md` (spec 0003, status `In Progress`, build tasks 1 to 7 complete, `/check verify` and `/test` still pending). Decision: extraction reuses the resume already staged from feature 06's upload-on-select flow (server mints a fresh signed URL and reads it) rather than re-uploading the file; the merge policy is a full overwrite of the extractable fields; Email and every Job Preferences field (job titles seeking, remote preference, salary expectation, preferred locations) are never touched, since a resume does not state them and the response schema has no slot for them at all. Built: `types/index.ts` gained `ExtractedProfileFields`; new `agent/resume-extractor.ts` (`extractProfileFromResumeText`, this project's first `agent/` function calling OpenAI's chat completions API directly rather than through Stagehand, `gpt-4o`, `temperature: 0.3`, `max_tokens: 800`, a `zod` schema with per-field `.catch()` coercion so one bad enum value from GPT-4o doesn't waste an otherwise good extraction, and `workExperience` capped to the first 3 entries via `.transform(...slice(0, 3))` rather than a `.max(3)` that would have wiped the whole array on a 4th entry); new `app/api/resume/extract/route.ts` (auth check, a `resumeKey` ownership check via the `${userId}/` prefix before any storage read, the empty/short extracted text guard, never writes to the database); `components/profile/ResumeUpload.tsx` gained `canExtract`/`isExtracting`/`onExtract`/`extractError` props and an Extract from Resume button, shown only once a resume is staged, disabled together with the dropzone and file input while either an upload or an extraction is in flight (a combined `isBusy` guard); `components/profile/ProfileEditor.tsx` gained `isExtracting`/`extractError` state, posts to the new route with the staged `resumeKey`, and merges a successful result onto `profile` by object spread; `saveDisabled` on `ProfileForm` now covers both `isUploading` and `isExtracting`. Added `openai`, `pdf-parse` (the currently installed major version, v2, has a completely different API from the `require('pdf-parse')(buffer)` shape in the stale `context/library-docs.md` entry: `new PDFParse({ data: buffer })`, `await parser.getText()`, `await parser.destroy()`; flagged as a Follow-up for `/sync`), and `zod` as new dependencies, all pre-approved. `tests/resume-extractor.test.mjs` (7 tests against the real `extractedProfileSchema`, including the enum-coercion-doesn't-lose-sibling-fields case and the workExperience-capping-doesn't-wipe-data case) and `tests/resume-extract-route.test.mjs` (6 source-contract tests: auth-before-storage, ownership-check-before-signed-url, never touches `insforge.database`, the exact empty-PDF error copy, try/catch with the `[resume/extract]` log prefix, `parser.destroy()` in a `finally`) are new; `tests/profile-contract.test.mjs` gained 2 new tests and had 2 existing ones updated for the new `isBusy`/`saveDisabled` wiring. Full suite: 109/109 passing. `npx tsc --noEmit` and `npm run lint` both clean. A live, unauthenticated smoke test against the route (both a real `resumeKey` body and an empty body) confirmed it responds `401` cleanly with no crash before any spec gap could surface; full behavioral verification (a real resume PDF extracting correctly, the empty-text error path, the ownership rejection) is left to `/check verify`.
- `/check verify` on 07 drove the real profile page end to end with a throwaway InsForge account (real signup, cookie-injected session, a real Playwright browser via `npx --package=playwright`, no project dependency added) and found a genuine bug: every real extraction attempt returned `500`, server log `Setting up fake worker failed: "Cannot find module '.../pdf.worker.mjs'..."`. Confirmed by direct evidence: the ownership check (`403` on a foreign resume key) and the never-writes-to-the-database invariant (`profiles` row absent after every attempt, confirmed via `db query`) both held; only the actual GPT-4o extraction path (AC-3, AC-4, AC-8) was broken. `/debug` found the root cause: `pdf-parse` v2 wraps `pdfjs-dist`, which needs its worker module registered as a real import, not resolved as a bundler chunk, or it fails exactly this way under Next.js's server bundler (confirmed against the library's own `docs/troubleshooting.md`, fetched directly, not assumed). Fixed with the two changes that doc specifies: `app/api/resume/extract/route.ts` now does `import "pdf-parse/worker"` before `import { PDFParse } from "pdf-parse"`, and `next.config.ts` gained `serverExternalPackages: ["pdf-parse"]`. Reproduced deterministically (uploaded a real resume PDF directly to storage under a throwaway account, hit the route with `curl` and a real session cookie), confirmed the exact same server log before the fix, and confirmed a real, well formed GPT-4o extraction succeeded after it, plus the empty-text guard (AC-4) correctly reached for a near-blank PDF. A regression test was added to `tests/resume-extract-route.test.mjs` asserting the import order and the `next.config.ts` declaration; confirmed it fails without the fix and passes with it. No sibling usages of `pdf-parse` exist elsewhere in the codebase. Full suite: 110/110 passing, `tsc`/`lint` clean. Both throwaway InsForge accounts used across the verify and debug passes (and their directly-uploaded storage objects, which don't cascade-delete since they weren't created through the app's own upload path) were deleted afterward; `require_email_verification` was restored to its original `true`. `/check verify 07` needs to re-run to formally close this out before `/test`.
- 08 Resume PDF Generation from Profile designed via `/architect` and recorded at `docs/specs/0004-resume-pdf-generation-from-profile/` (spec 0004, status `In Progress`, build tasks 1 to 8 complete, task 9's real signed in run plus `/check verify` and `/test` still pending). Decision: one `POST /api/resume/generate` route handler does the whole thing in a single request (read the saved profile, call GPT-4o, render the PDF, upload, write the DB pointer, delete the old key), the same shape as feature 07's extraction route; no background job. Two decisions confirmed with the engineer before the spec: the button gates on the profile having a full name and at least one work experience entry (client side hint, plus an independent server side re-check reading the DB row, not client state), and a successful generation shows an inline "View resume" control that mints a fresh signed URL only on click, never cached or stored. Generating necessarily overwrites the same `resume_pdf_url` column the feature 06/07 uploaded resume also uses, an accepted, already decided consequence. Built: `types/index.ts` gained `GeneratedResumeContent`; new `agent/resume-generator.ts` (`generateResumeContent`, `gpt-4o`, `temperature: 0.55`, `max_tokens: 1400`, a lenient `zod` schema plus a manual post validation reconciliation pass that maps returned bullet points back onto `profile.workExperience` by index and falls back to splitting that entry's own `keyResponsibilities` text if the model dropped or reordered one, so a role is never rendered empty and nothing is fabricated); new `app/api/resume/generate/ResumePdfDocument.tsx` (a `@react-pdf/renderer` `Document`, header/summary/skills/experience/education sections, only the CSS properties the library actually supports, co-located inside the route folder rather than `components/` or `agent/` so an accidental client import stays structurally awkward); new `app/api/resume/generate/route.ts` (auth check, reads the profile row fresh from the DB rather than trusting client state, the server side completeness gate, calls the agent function, renders the PDF via `React.createElement` since a Route Handler file must stay `.ts` and can't use JSX syntax directly, uploads to a fresh `${userId}/${randomUUID()}.pdf` key as a `Blob` since the InsForge SDK's `upload()` only accepts `File | Blob`, not a raw `Buffer`, writes the new key, deletes the previous key only after that write succeeds, `revalidatePath("/profile")`, and returns no key or URL in its response); new `app/api/resume/signed-url/route.ts` (a small `GET` endpoint, no client supplied key accepted at all, only ever looks up and mints a signed link for the caller's own `resume_pdf_url`, since no such on demand endpoint existed yet from features 06/07); `components/profile/ResumeUpload.tsx` gained `canGenerate`/`generateHint`/`isGenerating`/`onGenerate`/`generateError`/`generateSuccess`/`isFetchingViewLink`/`viewLinkError`/`onViewResume` props, wiring the previously dead Generate Resume from Profile button, and its combined `isBusy` guard now also covers generation; `components/profile/ProfileEditor.tsx` gained the matching state, `canGenerate` derived straight from current `profile` state, `handleGenerate`/`handleViewResume`, `router.refresh()` on a successful generation, and `saveDisabled` now also covers `isGenerating`. `tests/resume-generator.test.mjs`, `tests/resume-generate-route.test.mjs`, `tests/resume-signed-url-route.test.mjs`, and `tests/resume-pdf-template.test.mjs` are new; `tests/profile-contract.test.mjs` gained 6 new tests and had 2 existing ones updated for the new `isBusy`/`saveDisabled` wiring. Full suite: 146/146 passing. `npx tsc --noEmit` and `npm run lint` both clean. No live, signed in smoke test has run yet; that, plus the disabled/hint state and a real generate-then-view-then-regenerate cycle, is left to `/check verify`.
- 10 Adzuna Job Discovery designed via `/architect` and recorded at `docs/specs/0006-adzuna-job-discovery/` (spec 0006, status `In Progress`, all 7 build tasks complete, `/check verify` and `/test` still pending). Decision: `POST /api/agent/find` runs the whole search (Adzuna call, GPT-4o scoring per job, save every job regardless of score) in one request and returns only a summary; the page then refetches the saved rows directly from InsForge, reusing the query path feature 11 will extend with filtering, sorting, and pagination. Built: `types/index.ts` gained `JobMatch` and `JobRow`; new `lib/adzuna.ts` (`searchJobs`, `detectCountry`, a small hardcoded keyword lookup for gb/au/ca, defaulting to `us`); new `lib/match-score.ts` (the `getMatchScoreTier` display helper, moved out of the now deleted `lib/mock-jobs.ts`); new `agent/matcher.ts` (`scoreJobMatch`, `gpt-4o`, `temperature: 0.3`, `max_tokens: 300`, the same `zod` `.catch()` fallback pattern as `agent/resume-generator.ts`); new `agent/adzuna.ts` (`runJobSearch`, owns the `agent_runs` lifecycle, the search and score loop, every `jobs` insert, and the per job `job_found` PostHog event; a per job scoring failure still saves that job with a null `match_score` rather than dropping it); new `app/api/agent/find/route.ts` (auth check, `jobTitle` validation, a 422 gate when the caller's profile has no skills recorded, calls the orchestrator); `app/find-jobs/page.tsx` now also reads the caller's `profiles.skills` and passes `hasSkills`/`userId` down; `components/find-jobs/FindJobsPage.tsx` rewritten from feature 09's static mock shell to a real controlled form with a `status` state machine (`idle`/`loading`/`success`/`empty`/`error`), firing `job_search_started` client side and refetching real `jobs` rows via `insforge.database.from("jobs")` after a successful search. Removed as superseded: `lib/mock-jobs.ts` (the `MockJob` type and 6 row array) and `tests/mock-jobs.test.mjs`, since nothing imports the mock dataset anymore; its one still relevant test (the `getMatchScoreTier` boundaries) moved to the new `tests/match-score.test.mjs`. `tests/find-jobs-contract.test.mjs` updated throughout for the new controlled inputs, real fetch call, and status based gating, replacing the feature 09 assertions that specifically checked for unwired, static behavior. New `tests/adzuna-client.test.mjs`, `tests/matcher.test.mjs`, `tests/agent-adzuna.test.mjs`, `tests/agent-find-route.test.mjs`. `.env.example` gained the three previously undocumented but already live env vars: `OPENAI_API_KEY`, `ADZUNA_APP_ID`, `ADZUNA_APP_KEY`. Full suite: 203/203 passing. `npx tsc --noEmit -p .`, `npm run build` (with `/api/agent/find` listed as a route), and ESLint on every new/changed file all clean. No live, signed in smoke test has run yet; that, plus the zero results and Adzuna failure paths, is left to `/check verify`.
- `/check verify` on 10 confirmed the commands (203/203 tests, `tsc`, ESLint, `npm run build`) and, since no browser MCP or DB MCP is available in this environment, split the runtime proof two ways: AC-6 (unauthenticated `POST /api/agent/find` → 401, no row written) was confirmed directly via `curl` against the engineer's already-running dev server; AC-1, AC-2, AC-3, AC-4, AC-8 were confirmed by the engineer driving the real `/find-jobs` page themselves (signed in as a real Google OAuth account, no password to script against) and reporting back what rendered, cross-checked against the live database by querying InsForge's REST API directly with the project's admin key (`.insforge/project.json`), which is durable and worth remembering for any future verify pass that needs read access without a browser: a real search for "Software Engineer"/"US" wrote exactly 10 `jobs` rows, every one `source: "search"` with a populated `match_score`/`match_reason`/`matched_skills`/`missing_skills` and the correct `run_id`, and its `agent_runs` row showed `status: "completed"`, `jobs_found: 10`; a zero-result search wrote `jobs_found: 0` and zero `jobs` rows. `job_search_started` (AC-7) was confirmed twice over via the dev server's forwarded browser console log (Console Ninja), correct `userId`/`jobTitle`/`location` both times; `job_found`, emitted server side via `posthog-node`, had no console trace to cite and was left blocked, not asserted. AC-5 (a simulated Adzuna failure) was not exercised this pass, also left blocked. One odd thing the engineer flagged (fetch calls still firing after a search, even on `/profile`) turned out to be PostHog's own periodic session-recording `$snapshot` traffic, unrelated to this feature, confirmed in the same log. Verdict: PASS on every AC that was exercised; AC-5 and half of AC-7 remain open, noted rather than faked.
- `/test` found the existing suite (written during `/develop`, all source-contract style consistent with this project's convention of no network mocking) already covered nearly every branch, but added 7 new tests for genuine gaps: `tests/agent-adzuna.test.mjs` gained coverage for the `STRONG_MATCH_THRESHOLD = 70` boundary (a null score must count as 0, not throw) and `formatSalary`'s null/rounding/range behavior and the `contract_type` → `"fulltime"` fallback, none of which had any prior assertion; `tests/find-jobs-contract.test.mjs` gained coverage for the real `hasSkills` boolean derivation (AC-3 depends on this, previously only its downstream usage was tested), the client refetch's own error path, the outer try/catch around the search fetch, and the `found_at` descending sort. Full suite: 210/210 passing. Left deliberately uncovered: `job_found`'s runtime firing (no PostHog test double anywhere in this project) and `agent/matcher.ts`'s `jobMatchSchema` fallback behavior (not exported, only reachable via source pattern, already covered that way). Feature 10 is done: spec 0006 should move to `Accepted`.
- 14 Dashboard Page — Full UI designed, built, verified live, tested, and accepted in one session against a new spec, `docs/specs/0010-dashboard-page-ui/` (spec 0010, status `Accepted`). Replicated `context/designs/dashboard.png` pixel for pixel: four stat cards, a Recent Activity list, and three charts (Company Research Activity and Match Score Distribution as bar charts, Jobs Found Over Time as a filled area chart), all from a new `lib/mock-dashboard.ts`; fully replaced `app/dashboard/page.tsx`'s old empty-state placeholder shell (deleted `components/dashboard/DashboardActions.tsx` along with it, and trimmed the now stale posthog contract test that referenced it). `recharts` (v3.10.1) is this project's first charting dependency, chosen over visx and Chart.js for its SVG based, CSS variable friendly styling; every chart colors itself from the existing `--color-*` tokens, no new palette. The one real (non mock) piece is a conditional `IncompleteProfileBanner`, reusing `FindJobsPage`'s warning banner pattern, driven by a genuine `profiles` read and `isProfileComplete` (`lib/profile-completion.ts`), never by mock data. New: `components/dashboard/{StatCard,RecentActivityCard,CompanyResearchActivityChart,JobsFoundOverTimeChart,MatchScoreDistributionChart,IncompleteProfileBanner}.tsx`. Verified live end to end with two throwaway InsForge accounts (email/password signup, `email_verified` flipped directly via `npx @insforge/cli db query` since this sandbox has no OAuth creds, session cookie injected into a real Playwright browser, both accounts and their `profiles` rows fully deleted afterward): confirmed the incomplete and complete profile banner states, correct stat/activity/chart values matching the design exactly, no horizontal overflow or console errors at 390px, zero client side API calls beyond the one server side `profiles` read, and keyboard focus reaching the banner's link with a visible outline. `/check verify`'s only correction: `verify.md` assumed an unauthenticated visit would redirect to `/login?error=session`; actual behavior is plain `/login` since `proxy.ts` only forces the `?error=session` variant for `/find-jobs/[id]` and real session expiry, exactly matching every other protected page (not a regression). `/test` added `tests/mock-dashboard.test.mjs` (real imports, exact value assertions) and `tests/dashboard-page.test.mjs` (source-contract style, matching this project's established convention), 22 new tests, one real test bug caught and fixed during the run (a wrong regex, not a code defect). Full suite: 298/298 passing. `context/ui-registry.md`'s `DashboardPage` entry rewritten to reflect the new composition.
- 15 Stats Bar — Real Data built directly (no new spec: `context/build-plan.md`'s existing logic for this feature, four scoped COUNT/AVG queries, was already fully specified, no architectural decision to invent). `app/dashboard/page.tsx` now also reads the current user's `jobs` rows (`match_score, company_research, found_at`, scoped by `user_id`, the same query pattern `app/find-jobs/page.tsx` already uses) and passes them through a new pure function, `lib/dashboard-stats.ts`'s `computeDashboardStats`, which derives all four stats from that single query: Total Jobs Found (row count), Avg. Match Rate (mean of non-null `match_score`, rendered `"N/A"` rather than `NaN%` when nothing has been scored yet), Companies Researched (count of non-null `company_research`), Jobs This Week (`found_at` within the last 7 days of a passed-in `now`, defaulted to `new Date()` but parameterized for deterministic testing). `lib/mock-dashboard.ts`'s `mockStats` was removed as superseded, the same precedent as feature 10 deleting `lib/mock-jobs.ts`. One implementation-only judgment call, not a design decision: the mock stat cards had "vs last week" trend pills on two of the four cards, but the build plan gives no historical comparison data to compute a real trend from, so all four real stat cards now use a plain caption (`StatCard`'s existing trend-vs-caption branch already supported this, unchanged) rather than inventing or faking a trend number. `tests/dashboard-stats.test.mjs` is new (11 tests against the real pure function: stat order, the count/average/researched/this-week math together, the N/A fallback, the 7-day boundary, and the all-zero empty state). `tests/mock-dashboard.test.mjs` and `tests/dashboard-page.test.mjs` updated for the removed `mockStats` export and the new real query/composition. Full suite: 302/302 passing. `npx tsc --noEmit -p .`, `npm run lint`, and `npm run build` all clean (`/dashboard` still lists as a dynamic route). No live, signed in smoke test has run yet confirming the real numbers against a live account with actual `jobs` rows; left to `/check verify`.
- 16 Recent Activity — Real Data built directly, same shape as feature 15 (no new spec: `context/build-plan.md`'s existing logic, merge two scoped queries and format, was already fully specified, no architectural decision to invent). `app/dashboard/page.tsx` now also reads the current user's completed `agent_runs` (`id, job_title_searched, jobs_found, completed_at`, `.eq("status", "completed")`) and `jobs` rows with a non null `company_research_completed_at` (`id, company, company_research_completed_at`, `.not("company_research_completed_at", "is", null)`), both scoped by `user_id`, ordered newest first, capped at 10 rows each. New `lib/dashboard-activity.ts`: `computeRecentActivity(agentRuns, researchedJobs, now?)` merges the two sources into `DashboardActivityEntry[]`, sorted by timestamp descending and capped to the newest 8 (within the build plan's specified 5 to 10 range); an agent run becomes `"Found {jobsFound} jobs for {jobTitleSearched}"` with a `success` dot, a researched job becomes `"Researched {company}"` with an `info` dot, following the build plan's explicit two color rule (`accent`, the type's third color, stays unused by real data, same precedent as feature 15 leaving `StatCard`'s trend branch unused). Its own `formatTimeAgo(timestamp, now?)` reproduces the mock's exact phrasing (`"Just now"`, `"N min(s) ago"`, `"N hour(s) ago"`, `"Yesterday"`, `"N days ago"`) from a raw ISO timestamp, parameterized on `now` for deterministic testing, same pattern as feature 15's `computeDashboardStats`. `lib/mock-dashboard.ts`'s `mockActivity` was removed as superseded; its `DashboardActivityEntry` type stays, now shared by the real data path and unchanged by `RecentActivityCard`. Added `types.AgentRunRow` (no such type existed yet). `tests/dashboard-activity.test.mjs` is new (10 tests: every `formatTimeAgo` boundary, the real merge-and-sort behavior, the 8 entry cap, the null-timestamp exclusion for both sources, the missing-data fallback title, and the empty-activity case). `tests/mock-dashboard.test.mjs` and `tests/dashboard-page.test.mjs` updated for the removed `mockActivity` export and the new real query/composition (including the fourth `.from()` call, `agent_runs`, now expected alongside the repeated `jobs` call). Full suite: 306/306 passing. `npx tsc --noEmit -p .`, `npm run lint`, and `npm run build` all clean (`/dashboard` still lists as a dynamic route). `/check verify` on 16 passed live against a throwaway account (deleted afterward, config restored) seeded to make a wrong implementation visible rather than just exercising the happy path: 5 completed `agent_runs` and 5 researched `jobs` interleaved in time, plus one `running` run recent enough to sit near the top and one job carrying `company_research` with a null `company_research_completed_at`. The rendered list alternated run, research, run, research in exact timestamp order, proving the two sources are genuinely merged and sorted rather than concatenated; showed 8 of the 10 eligible entries, dropping exactly the two oldest, confirming the cap; excluded both the `running` run and the null timestamp job; and used the green `success` dot for runs and the blue `info` dot for research throughout. Time phrasing covered the minutes, hours, Yesterday, and days branches (the `Just now` branch was not observed live, since roughly 90 seconds of drift between seeding and render pushed the newest entry to "2 mins ago"; it is covered by unit test). Cross user isolation was proved rather than assumed: 5 `agent_runs` and 4 researched `jobs` belonging to other users existed in the database at the time, and none appeared. Zero console errors.
- 17 Analytics Charts — Real Data built via `/develop` against `docs/specs/0011-analytics-charts-real-data/` (spec 0011, status `Accepted`, all 5 build tasks complete, `/check verify` and `/test` both passed). Follows spec 0011's decision to query InsForge (Postgres) directly rather than reading PostHog back out, since `found_at`, `match_score`, and `company_research_completed_at` were already present on the same `jobs` rows this page fetches (features 15/16). `app/dashboard/page.tsx`'s existing `statsJobs` select grew to also fetch `company_research_completed_at` (still one scoped query). New `lib/dashboard-charts.ts`: three pure functions, `computeJobsFoundOverTime` (30 day UTC window, zero filled, short date labels e.g. "Jul 15"), `computeMatchScoreDistribution` (all time, 5 bands 50-60% through 90-100%, lower inclusive/upper exclusive except the top band, nulls and sub-50 scores excluded), and `computeCompanyResearchActivity` (rolling 7 day UTC window, zero filled, weekday abbreviation labels), sharing a `computeZeroFilledWindow` helper for the two day-bucketed charts, all parameterized on `now: Date` for deterministic testing, same pattern as `lib/dashboard-stats.ts`/`lib/dashboard-activity.ts`. `JobsFoundOverTimeChart`, `MatchScoreDistributionChart`, and `CompanyResearchActivityChart` each gained an empty state (the `FindJobsPage` `bg-surface-secondary`/`role="status"` pattern) shown when the chart's total count is 0, and `JobsFoundOverTimeChart`'s `XAxis` gained `interval={4}` so its 30 short date ticks don't collide. `lib/mock-dashboard.ts`'s `mockJobsFoundOverTime`, `mockMatchScoreDistribution`, and `mockCompanyResearchActivity` are removed (types stay); `tests/mock-dashboard.test.mjs` (tested only those three removed exports) was deleted, and `tests/dashboard-page.test.mjs` updated for the grown select string, the new `dashboard-charts` import/wiring assertions, and the real prop names in the composition test. Full suite: 304/304 passing at build time. `npx tsc --noEmit -p .` and `npm run lint` both clean.
- `/check verify` on 17 drove the real `/dashboard` with two throwaway InsForge accounts (email/password signup, `require_email_verification` toggled off via `insforge.toml` and restored afterward, session cookie injected into a real Playwright browser, both accounts plus their seeded `jobs`/`profiles` rows deleted afterward): one account seeded with 12 jobs spread across the window boundaries confirmed all three charts render real values matching a manual count (30 point line with "Jul 3" to "Jul 28" labels, 5 bars at 1/2/2/2/2, 4 weekday bars), and a second account with zero rows confirmed all three empty states independently, zero console errors in both. A job seeded at 31 days old and one at 8 days old were correctly excluded from the 30 day and 7 day windows respectively, proving the boundaries for real rather than by inspection. Verdict: PASS on all 8 ACs. One incidental, pre-existing finding not introduced here: the "Companies Researched" stat card (feature 15) counts non-null `company_research` (the dossier jsonb) while features 16 and 17 count non-null `company_research_completed_at` (the timestamp), so a row carrying one without the other is counted inconsistently; latent today since feature 13's route always writes both together.
- `/check review` ran on Opus (author was Sonnet) and wrote `docs/reviews/2026-07-31-dashboard-page.md`: verdict Changes requested, 0 blockers, 2 majors, 7 minors, 5 nits. Both majors fixed: `lib/dashboard-charts.ts` had no unit tests at all, and every dashboard read discarded its `error` so a failed query rendered as feature 17's new "you have no data" empty states with nothing logged (materially worse than feature 15, where a failure at least showed obviously wrong zeros). All four reads on `app/dashboard/page.tsx` now destructure `error` and log it with the `[app/dashboard]` prefix, per `code-standards.md`'s existing rule. The two display level minors were fixed in a follow up pass: `interval={4}` rendered ticks at indices 0/5/10/15/20/25, so the 30 day axis never labeled today and read as ending five days early, now `interval="preserveStartEnd"` (recharts' own documented auto option for overlapping axis labels, confirmed present in the installed v3 `AxisInterval` type); and `utcDateKey` called `.toISOString()` unguarded, so one unparseable timestamp threw `RangeError` and failed the whole page render, now split so row timestamps go through `rowDateKey`, which returns null for an unparseable value and drops just that row, matching how `dashboard-stats.ts` and `dashboard-activity.ts` already degrade. Both fixes were proven rather than assumed: the old `utcDateKey` was executed directly on the new test's input to confirm it really throws `RangeError`, and the axis was checked in a real browser against a throwaway account seeded with one job on all 30 days, reading the rendered tick labels out of the DOM. Before the fix the axis ran "Jul 3 … Jul 28", after it runs "Jul 3 … Aug 1" with today labeled at the right edge and no overlap. Suite: 327/327. Remaining review items are the 5 nits plus the minors about window semantics and naming (the UTC chart window versus feature 15's rolling 168 hour "Jobs This Week", the `company_research` versus `company_research_completed_at` split, `lib/mock-dashboard.ts` still being the home of the shared types, and the four serial reads that could be a `Promise.all`), all still open by choice.
- `/test` added `tests/dashboard-charts.test.mjs` (14 unit tests against the real pure functions: window widths, zero fill, UTC bucketing, the inclusive window start versus one millisecond before it, future and long past rows dropped, weekday labels, every band boundary, 100 in the top band, null and sub 50 exclusion) and extended `tests/dashboard-page.test.mjs` by 7 source contract tests (the three empty states and their per chart copy, the empty state replacing rather than accompanying the chart, tick thinning on only the 30 point chart, `mock-dashboard.ts` keeping its types while staying mock free, no PostHog path in the compute module, and the new error logging on all four reads). `tests/mock-dashboard.test.mjs` was deleted, its three subjects no longer exist. Because a suite that passes first try can pass vacuously, both new test sets were mutation checked rather than trusted: an off by one on the window start fails 8 tests, swapping the UTC day key for a local one fails 4, a band upper edge flip fails 2, dropping 100 from the top band fails 1, and removing the `timeZone: "UTC"` label pin fails 1; separately, all five load bearing source contract assertions were confirmed to fail against the real pre change code from git. One mutant (removing the explicit null guard in `computeMatchScoreDistribution`) failed nothing and was confirmed an equivalent mutant, not a test gap, since `null >= 50` is already `false` in JavaScript. Full suite: 325/325 passing, `tsc` and lint clean. All 8 ACs trace to automated tests.
- 13 Company Research Agent designed and built via `/develop` against a new spec, `docs/specs/0009-company-research-agent/` (spec 0009, status `In Progress`). The engineer's own plan already carried the load bearing decisions (Stagehand schemas, GPT-4o synthesis prompt, dossier shape) from `context/build-plan.md`; the spec formalized three previously open panels using the plan's recommended defaults: the Research Company button is synchronous (awaits the request, then `router.refresh()`s), a job with a saved dossier shows it directly with no refresh action in this slice, and a new `jobs.company_research_completed_at timestamptz` column (migration `20260731180810_add-jobs-company-research-completed-at.sql`, applied via `npx @insforge/cli db migrations up --all`) closes the exact gap `docs/specs/0001-database-schema/index.md` had already flagged for feature 16. Built: `types/index.ts` gained a named `CompanyResearchDossier` type (`JobRow.company_research` is now typed to it instead of the feature 12 placeholder `Record<string, unknown> | null`) and `JobRow.company_research_completed_at`; new `agent/research.ts` (`runCompanyResearch`, `deriveCompanyHomepageUrl` follows the saved redirect via `fetch(..., { redirect: "follow" })` and strips to the root domain, or falls back to a `https://www.{company}.com` guess; one bounded `@browserbasehq/stagehand` v3 session, homepage extract plus up to 3 sub pages capped and prioritized by link kind, the session always closed in a `finally`; GPT-4o synthesis with a `zod` schema for the nine dossier fields, always returning a complete dossier even when browsing fails or is thin, never a partial or invented one); new `app/api/agent/research/route.ts` (auth check, `jobId` validation, the same `id`+`user_id` scoped read pattern as the feature 12 details route, falls back to `buildEmptyProfile` when no profile row exists yet, writes `company_research`+`company_research_completed_at` together only after a successful agent result, fires `company_researched`, `revalidatePath("/find-jobs/[id]")`); `components/job-details/CompanyResearchCard.tsx` is now a client component with idle/loading/error/saved states, replacing feature 12's permanently disabled placeholder. Stagehand's actual installed API (v3, `@browserbasehq/stagehand@3.7.1`) turned out to differ from the object-form `stagehand.extract({ instruction, schema })` example in `context/build-plan.md`: the real signature is positional, `stagehand.extract(instruction: string, schema: ZodSchema, options?)`, confirmed directly against the installed package's `.d.ts` files rather than assumed from training data or the plan's own snippet; implemented against the real signature. New tests: `tests/research-agent.test.mjs` and `tests/agent-research-route.test.mjs` (source-contract style, matching every other agent/route test in this suite); `tests/job-details.test.mjs` updated throughout for the new `CompanyResearchCard` behavior, replacing the feature 12 "stays a disabled empty state" regression test with real idle/loading/error/saved-dossier coverage. Full suite: 270/270 passing. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/agent/research` listed as a route). Confirmed live via `curl` against the engineer's own running dev server: an unauthenticated `POST /api/agent/research` returns `401`, and an unauthenticated `/find-jobs/[id]` visit redirects to `/login?error=session`. No real signed in run has happened yet — the engineer explicitly chose to leave that open rather than have it faked, since this environment has no OAuth test credentials (the same gap noted for feature 02's auth verify). `docs/specs/0009-company-research-agent/index.md`'s AC-13 (a real Research Company click plus a saved-dossier reload, at desktop and mobile widths) is the one item left for a human-driven `/check verify` pass before spec 0009 moves to `Accepted` and this checkbox is ticked.

---

## Notes

- Add `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_INSFORGE_URL`, and `NEXT_PUBLIC_INSFORGE_ANON_KEY` to `.env.local` before testing OAuth.
- Add `NEXT_PUBLIC_POSTHOG_KEY` and `NEXT_PUBLIC_POSTHOG_HOST` to `.env.local` before testing analytics.
- Production build verification is pending because the sandbox could not fetch Inter from Google Fonts. Auth tests, TypeScript, and ESLint pass.
